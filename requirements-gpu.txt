# Full stack for the RunPod GPU box (install torch via setup_runpod.sh first).
# Cap below 5: CheXagent's trust_remote_code modeling imports symbols removed in
# transformers 5.x (e.g. find_pruneable_heads_and_indices from pytorch_utils).
# LLaVA-Med (HF-native Llava class) works on 4.x too, so 4.x satisfies both.
transformers>=4.49,<5
accelerate
datasets
# RunPod/cloud GPU images commonly set HF_HUB_ENABLE_HF_TRANSFER=1; without this
# package, downloads from non-Xet repos (e.g. CheXagent) hard-fail. Unused/harmless
# if the flag is not set. Alternatively export HF_HUB_ENABLE_HF_TRANSFER=0.
hf_transfer
pillow
numpy
pandas
pyyaml
sentencepiece
protobuf
einops
sentence-transformers
# Unified radiology report metrics: F1CheXbert, F1RadGraph, GREEN.
# Confirm the call signature against the installed README (API varies by version).
radeval
